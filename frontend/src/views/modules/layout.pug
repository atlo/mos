block vars
    - var pageClass = 'media-owner'

html
    head
        meta(charset="utf-8")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        //- script(src="https://d3js.org/d3.v4.min.js")
    body(class=pageClass)
        block content
        //- Comment below needed for SVG include task
        // SVG-SPRITE
